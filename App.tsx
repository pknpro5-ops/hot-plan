
// Файл больше не используется как корневой компонент. 
// Весь контент перенесен в index.html для SEO.
// Интерактив (Квиз) монтируется напрямую в index.tsx.
export default function App() { return null; }
